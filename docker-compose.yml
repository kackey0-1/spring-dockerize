version: '2'
services:
  dockerize-spring:
    container_name: dockerize-spring
    build: ./
    ports:
      - 8080:8080
    networks:
      - spring-cloud-network
networks:
  spring-cloud-network:
    driver: bridge